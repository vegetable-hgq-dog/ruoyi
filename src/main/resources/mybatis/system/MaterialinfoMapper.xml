<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.project.system.materialinfo.mapper.MaterialinfoMapper">
    
    <resultMap type="Materialinfo" id="MaterialinfoResult">
        <result property="materialid"    column="materialID"    />
        <result property="materialname"    column="materialName"    />
        <result property="materialtype"    column="materialType"    />
    </resultMap>

    <sql id="selectMaterialinfoVo">
        select materialID, materialName, materialType from materialinfo
    </sql>

    <select id="selectMaterialinfoList" parameterType="Materialinfo" resultMap="MaterialinfoResult">
        <include refid="selectMaterialinfoVo"/>
        <where>  
            <if test="materialname != null  and materialname != ''"> and materialName like concat('%', #{materialname}, '%')</if>
            <if test="materialtype != null  and materialtype != ''"> and materialType = #{materialtype}</if>
        </where>
    </select>
    
    <select id="selectMaterialinfoById" parameterType="Long" resultMap="MaterialinfoResult">
        <include refid="selectMaterialinfoVo"/>
        where materialID = #{materialid}
    </select>
        
    <insert id="insertMaterialinfo" parameterType="Materialinfo" useGeneratedKeys="true" keyProperty="materialid">
        insert into materialinfo
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="materialname != null  and materialname != ''">materialName,</if>
            <if test="materialtype != null  and materialtype != ''">materialType,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="materialname != null  and materialname != ''">#{materialname},</if>
            <if test="materialtype != null  and materialtype != ''">#{materialtype},</if>
         </trim>
    </insert>

    <update id="updateMaterialinfo" parameterType="Materialinfo">
        update materialinfo
        <trim prefix="SET" suffixOverrides=",">
            <if test="materialname != null  and materialname != ''">materialName = #{materialname},</if>
            <if test="materialtype != null  and materialtype != ''">materialType = #{materialtype},</if>
        </trim>
        where materialID = #{materialid}
    </update>

    <delete id="deleteMaterialinfoById" parameterType="Long">
        delete from materialinfo where materialID = #{materialid}
    </delete>

    <delete id="deleteMaterialinfoByIds" parameterType="String">
        delete from materialinfo where materialID in 
        <foreach item="materialid" collection="array" open="(" separator="," close=")">
            #{materialid}
        </foreach>
    </delete>

    <select id="selectMaterialName"  resultMap="MaterialinfoResult">
        select materialName from MaterialInfo
    </select>

</mapper>